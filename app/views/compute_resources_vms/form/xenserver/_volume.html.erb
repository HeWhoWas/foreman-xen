<%
   attribute_map = compute_attribute_map(params, compute_resource, @host)
   storage_repositories = xenserver_storage_repositories(compute_resource)
%>

<%= selectable_f f, :sr_uuid, storage_repositories,
                 { },
                 :class => "form-control span5",
                 :label => _("Storage Repository") %>

<%= text_f f, :size_gb,
           :class       => "col-md-2",
           :label => _("Size (GB)") %>
